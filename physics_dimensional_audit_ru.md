# Аудит физического ядра: размерности, единицы, согласованность

Дата: 2026-07-16. Предмет: `utils/physics.py` (`PurePDEKernel`) и обучаемый
двойник `utils/physics_hybrid.py` (`PDE_kernel`). Метод: не сверка «на глаз»,
а инстанцирование ядра на USA-подобной сетке (32×64, 13 уровней) и прогон через
каждый член физически-масштабированных полей ERA5 с проверкой единиц, знаков и
переводов гПа↔Па.

Итог: **уравнения размерно согласованы и знаки верны**. Найдена одна реальная
систематическая погрешность в вертикальной квадратуре (краевые слои `M_z`),
общая для обоих модулей, плюс несколько заметок о дефолтах.

---

## 1. Что проверено и подтверждено ✅

### 1.1. Знаковые конвенции вертикали (аудит 13) — все согласованы

| Проверка | Ожидание | Численный результат | Статус |
|---|---|---|---|
| `d_z` на аналитическом `f=p` | −∂/∂p ≈ −1 (1/гПа) в середине столба | −1.00 в центре, ошибка 5–8% на скачках шага 300→400 и 700→850 | ✅ знак верен |
| `get_w` при конвергенции ветра (∂u/∂x<0) | подъём, w>0 (вверх-полож.) | w>0 в точке конвергенции, w<0 при дивергенции | ✅ |
| ω = −100·w | подъём ⇒ ω<0 | ω<0 при подъёме, ~0.54 Па/с rms (реалистично) | ✅ |
| Адиабата `R_d·T·ω/(c_p·p)` при подъёме | охлаждение dT/dt<0 | <0 (−123 K/сут при ω=−1 Па/с) | ✅ |
| Гидростатика `get_z_t` при прогреве (t_t>0) | z растёт, z_t>0 | z_t>0, максимум на топе (наибольший интеграл) | ✅ |
| Конденсация: δ=1 при подъёме (ω<0) и q≥q_s | сток q≤0 | триггер и знак верны | ✅ |

Все знаковые исправления B1/B2/B3 из аудита 13 присутствуют и работают. В
`_si_setup` (semi-implicit) оба флипа (ω=−100·w и +R/p·t_t) явно зеркалят
фактическую цепочку RHS и взаимно гасятся — оператор A численно консистентен.

### 1.2. Переводы единиц гПа↔Па — консистентны внутри каждого члена

Ключевой тонкий момент: **разные члены делят на давление в разных единицах, и
это правильно**, потому что каждый согласован со своей переменной интегрирования:

- **Гидростатика** (`get_z_t`): `z_zt = R_eff/p_гПа · t_t`, затем `integral_z`
  с `M_z` в **гПа**. Отношение dp/p безразмерно ⇒ делитель тоже в гПа
  (`grid.pressure` в Па → `/100`). Единицы: R_eff·t_t = [Дж/(кг·К)]·[К/с] =
  м²/с³; интеграл по dp/p безразмерен ⇒ z_t в **м²/с³**. ✅
- **Адиабата** (`get_t_t`): `R_d·T·ω_Па/(c_p·p_Па)`. ω в Па/с, p в Па ⇒
  ω/p в 1/с; R_d·T/c_p в К ⇒ dT/dt в **К/с**. ✅
- **Конденсация** (`_condensation_source`): `δ·F·ω_Па/p_Па`. Размерный разбор
  F: числитель и знаменатель оба [Дж²/(кг²·К)] и [Дж²/кг²] ⇒ F безразмерен
  (кг/кг); ω/p в 1/с ⇒ dq/dt в **(кг/кг)/с**. ✅ `_get_qs` вызывается с
  `grid.pressure` в **Па** — согласовано с формулой Магнуса (e_s в Па). ✅

`physics_hybrid.PDE_kernel` использует **те же самые** переводы (гидростатика
делит на гПа, адиабата и конденсация на Па: строки 699, 719, 735, 841, 849).
Численного расхождения между модулями по единицам нет.

### 1.3. Формула Магнуса и q_s — численно точна

`q_s`(T=15°C, 1000 гПа) = **0.01067 кг/кг** против референса ≈0.0107 — совпадение.
e_s = 6.112·exp(...)·100 = 611.2 Па при 0°C (правильно). Поэлементный `clamp(±20)`
вместо batch-global remap — корректный числовой guard.

### 1.4. Кориолис — f = 2Ω·sin(φ) верен

`f_spherical` на USA-широтах: 5.93e-5 … 1.35e-4 1/с (диапазон 24–68°N) —
физически корректно. Множитель 2 присутствует во всех вариантах.

### 1.5. Геометрия боевого пути — размерно корректна

Деплой-путь (`physics_residual` → `PDE_kernel`) размерно чист:

- Горизонтальные производные используют **crop-aware** буферы инстанса
  (`self.pixel_x = R·cosφ·Δλ`, `self.pixel_y = R·Δφ`), а НЕ легаси
  модуль-глобали (строка 37, старая формула `2πR·cosφ/16`). Проверено:
  `d_x_weno(dx=self.pixel_x)`, `d_y_weno(dy=self.pixel_y)`.
- Вертикальные операции (`d_z`, `integral_z`, `pressure`, `M_z`) — по уровням
  давления, от кропа не зависят, общие для обоих модулей.
- Конфиг B1/A1/A2 передаёт `physics_dlat_deg=5.625`, `physics_dlon_deg=5.625`
  = 4×1.40625° (patch-factor 4, `grid_h=H_data//4`), что соответствует
  физике на латентах 8×16. `physics_residual` **поднимает ошибку**, если
  оставлены баговые дефолты (−70/20/22.5), — баги геометрии G2/G3 из
  `equations.md` в деплое закрыты. ✅

---

## 2. Находка: краевое смещение вертикального интеграла `M_z` ❗

**Единственная реальная численная погрешность.** `pixel_z` задаёт толщину слоя,
приписываемую каждому уровню в квадратуре `integral_z`. Для **10 из 13
уровней** это точная центрированная (трапецеидальная) толщина
(Δp_below+Δp_above)/2. Расходятся три уровня — **два крайних** берут полный
соседний зазор вместо половинного, и **850 гПа** округлён на −0.5 гПа:

| Уровень | `pixel_z` (код) | Должно быть (центр.) | Ошибка |
|---|---|---|---|
| 50 гПа (топ) | 50 | 25 | **+25 гПа** |
| 850 гПа | 112 | 112.5 | −0.5 (округление) |
| 1000 гПа (поверхность) | 75 | 37.5 | **+37.5 гПа** |

Доминируют два крайних уровня (+62.5 гПа); вклад 850 гПа пренебрежим.

Сумма весов = **1012 гПа** против истинной глубины столба p_s−p_top = **950 гПа**
(**+6.5%** массы столба). Последствия:

1. **Якорь гидростатики Φ_t(p_s)=0 нарушен.** Документация (`equations.md`)
   и docstring `get_z_t` утверждают закрепление геопотенциала у поверхности.
   Фактически `integral_z(поле)` на приземном уровне даёт `поле·75гПа ≠ 0`
   (появляется фиктивный приземный слой). Численно: при равномерном прогреве
   z_t у поверхности = 0.0021 м²/с³ вместо 0.
2. **Смещение `get_z_t` и `get_w`.** Это следствие относится к другой
   величине, чем таблица `pixel_z` выше: к *кумулятивной глубине
   интегрирования* — сумме весов `M_z` от данного уровня до поверхности
   (`integral_z(ones)`), а не к поуровневой толщине. Из-за завышенных краевых
   весов эта кумулятивная глубина систематически больше истинной p_s−p:

   | Уровень | кумулят. код | кумулят. истина (p_s−p) | смещение |
   |---|---|---|---|
   | 50 гПа | 1012 | 950 | +62 (+7%) |
   | 500 гПа | 587 | 500 | +87 (+17%) |
   | 850 гПа | 262 | 150 | +112 (+75%) |
   | 1000 гПа | 75 | 0 | +75 (предельный: истина=0) |

   (Совпадение чисел «112» и «75» со значениями `pixel_z` в §2 — случайно;
   здесь это накопленные суммы, а не толщины отдельных слоёв.) В
   относительном выражении смещение монотонно растёт к поверхности. Итог:
   диагностическая ω и приземная z-тенденция систематически завышены по
   амплитуде, сильнее всего у поверхности — что подтверждает тест §2.1:
   z-residual на 1000 гПа падает на −91% после фикса.
3. Общая для `physics.py` и `physics_hybrid.py` (идентичное построение `M_z`).

**Оговорка о серьёзности.** Это ~2-й порядок квадратуры на грубой 13-уровневой
сетке; `equations.md` уже помечает `integral_z` как «метод прямоугольников,
~2-й порядок». Для чистого диагностического ядра (residual «физика vs ERA5»)
смещение входит в измеряемую ошибку; для обучаемого приора корректор частично
его поглощает. Но заявленный в документации точный приземный якорь фактически
не выполняется — это расхождение намерения и реализации.

**Рекомендация (минимальная правка, не по умолчанию — во избежание слома
регрессий).** Дать `Grid` opt-in `vertical_quadrature='trapezoid'`. Важно
различать две величины (они называются «вес», но участвуют в разных местах):

- **Строка `M_z` (матрица кумулятивного интеграла).** Приземная строка
  (i=P−1) — **нулевая**, так как ∫_{p_s}^{p_s}=0. Именно это даёт точный якорь
  `integral_z(f)[surface]=0`. Верхняя и приземная *краевые* толщины в остальных
  строках берутся половинными (топ 25, поверхность 37.5 вместо 50/75).
- **Вектор поуровневых весов `w_int`** (для колоночного усреднения дивергенции
  в `mass_consistent` ω) — центрированная полутолщина каждого уровня; на
  поверхности это **половина последнего зазора = 37.5 гПа** (ненулевая),
  сумма по столбу = 950 гПа.

То есть приземный вес = 0 в `M_z` (анкер) и = 37.5 гПа в `w_int` (толщина слоя)
— это не противоречие, а две роли, которые в легаси-коде обе брались из одного
завышенного `pixel_z`. Проверять тестом: `integral_z(ones)[surface] == 0`,
`integral_z(ones)[top] == p_s − p_top == 950` и `sum(w_int) == 950`.

---

### 2.1. Проверка на реальном срезе ERA5 (кластер, job 4180641)

Тест на реальных данных: 12 последовательных почасовых кадров USA-мемпапа
`predformer_usa_2000_2004.dat` (i0=12000, лето 2-го года, dt=3600 с), широты
кропа 16.17–59.77°N (rows 75..106), r→q через `relative_to_specific_humidity`.
Сравнение текущего ядра против трапецеидально-исправленного (половинные краевые
веса `M_z`/`pixel_z`). Ядро: `coriolis="spherical"`, `rows_south_to_north=True`,
`advection_form="flux"`, `w_diagnostic="mass_consistent"`.

**Приземный якорь гидростатики Φ_t(p_s)=0:**

| | z_t на 1000 гПа (м²/с³) |
|---|---|
| Текущее ядро | **0.108** (якорь нарушен) |
| Трапецеидальный фикс | **0.0** (якорь точный) |

**PDE-residual против реального следующего кадра ERA5** (широтно-взвешенный RMS,
cos φ; сравнение `(X_next−X_now)/dt − RHS_X`):

| Перем. | \|ERA5 dX/dt\| | resid текущий | resid трапец. | Δ |
|---|---|---|---|---|
| z | 1.28e-2 | 2.300 | 2.140 | **−7.0%** |
| t | 1.01e-4 | 6.51e-3 | 6.58e-3 | +1.0% |
| u | 2.85e-4 | 2.23e-3 | 2.23e-3 | −0.2% |
| v | 3.16e-4 | 5.35e-4 | 5.31e-4 | −0.9% |
| q | 5.53e-8 | 9.80e-8 | 9.22e-8 | **−5.9%** |

**z-residual по уровням** (широтно-взвешенный RMS, м²/с³) — эффект сосредоточен
у поверхности, где смещение глубины столба максимально:

| p (гПа) | \|ERA5 dz/dt\| | resid текущий | resid трапец. |
|---|---|---|---|
| 50 | 1.59e-2 | 4.151 | 3.999 (−3.7%) |
| 200 | 1.48e-2 | 3.049 | 2.776 (−9.0%) |
| 500 | 1.16e-2 | 1.069 | 0.875 (−18%) |
| 850 | 9.72e-3 | 0.365 | 0.209 (**−43%**) |
| 1000 | 9.93e-3 | 0.105 | 0.0099 (**−91%**) |

Диагностическая ω=−100·w: текущая 0.271 → трапец. 0.242 Па/с (обе в
реалистичном диапазоне ERA5 0.1–0.5).

**Выводы теста.**
1. Фикс делает приземный якорь Φ_t(p_s)=0 точным (0.108 → 0), как заявлено в
   документации, но фактически не выполнялось.
2. z-residual падает на 7% в целом и на **43–91% в нижней тропосфере**
   (850–1000 гПа), с максимумом **−91% у поверхности (1000 гПа)** — там, где
   краевой вес `pixel_z` завышен сильнее всего (+37.5 гПа, см. §2) и приземный
   якорь Φ_t(p_s)=0 нарушался. q-residual падает на 5.9%.
3. u/v/t почти не затронуты (<1%): их тенденции не проходят через колоночный
   гидростатический интеграл. Общий z-residual остаётся большим (2.3 против
   \|ERA5\| 0.013) — это **структурная неполнота** уравнений (z следует за T,
   ω-шум), а НЕ квадратурный баг; фикс её не устраняет, но снимает лишний
   систематический слой у поверхности.

Скрипт теста: `era5_quadrature_test.py` (артефакт сессии).

---

## 3. Заметки о дефолтах (не ошибки, но стоит знать)

- **`PurePDEKernel` дефолтит `coriolis="constant"`** (f=2Ω·sin45°≈1.03e-4,
  постоянный) — теряет широтную зависимость. Обучаемый `PDE_kernel` и
  деплой-путь дефолтят `"spherical"` (верно). Для диагностики на реальных
  широтах передавайте `coriolis="spherical"` явно.
- **`PurePDEKernel` дефолтит `rows_south_to_north=None`** (легаси d_y = −∂/∂y).
  Данные WeatherBench идут юг→север, поэтому для диагностики на сырых данных
  обязателен `rows_south_to_north=True` (иначе меридиональный барический
  градиент в v-уравнении перевёрнут — баг G1). В деплой-пути это уже учтено.
- **Дефолт `w_diagnostic="plain"`** шумен; боевые конфиги A1/A2 используют
  `mass_consistent` — это верный выбор (см. runbook / PHYSICS_STATS_2000).

---

## 4. Что осталось за рамками (не проверялось численно здесь)

- Внутренняя точность WENO-5 flux на разрывах (документировано: только для
  гладких полей, ошибка 3.7e-6 на sin).
- Устойчивость semi-implicit оператора на длинном роллауте (модальный базис
  roundtrip-exact по построению, но спектральные свойства на данных — отдельно).
- Диабатический член Q_θ (обучаемая надстройка, не в ядре).

Вывод: **уравнения размерно и по знакам корректны и взаимно согласованы**;
единственная предметная правка — половинные краевые веса в `M_z` для точного
приземного якоря и снятия +6.5% смещения массы столба.
