# Changelog

Формат — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/).

## [Unreleased]

### Removed

- **Эксперимент 18 (послойный разбор вклада физики) — ОТОЗВАН.**
  `docs/experiments/18_level_resolved_physics/` (текст сохранён с уведомлением об
  отзыве в §0). Его вывод «физика улучшает уровень ⇔ невязка уравнения там
  `residual_rel ≲ 1`» не воспроизводится: разбор шёл на промежуточной эпохе 90
  недоученной long-волны и мерил положение армов на траектории оптимизации, а не
  свойство физики. На плато валидации (пара R3↔R0 с совпадающей эпохой 270) знаки
  перевернулись: погранслой стал зоной наибольшего выигрыша (T@850 +0.4 % → −3.8 %),
  «влажностный налог» исчез (r@600 +1.9 % → −4.6 %), удар R3q по T@850 сменился
  помощью (+5.5 % → −6.1 %), а корреляции-опоры (u +0.58, z −0.68) схлопнулись к
  нулю — и были незначимы изначально (n = 13 уровней → порог |r| ≥ 0.55).
  **Уцелели:** невязки уравнений на ERA5 (свойство данных/уравнений), структурный
  вред легаси-физики (R1), несущая роль диабатика Q_θ. **Снято с программы:** рычаг
  «маска уровня» (активно вреден) и обучаемый гейт t/q; мотивация эксперимента 19
  помечена недействительной (фаза B не запускалась, компьют не потрачен).

### Added

- **Карта архитектуры PI-IAM4VP** — [docs/architecture.md](docs/architecture.md):
  mermaid-схема одного шага прогноза и схема PDE-ядра, формы тензоров, раскладка
  69 каналов, знаковые конвенции вертикальной оси. Ключевое, что документ
  фиксирует: физика входит в модель **как признак, а не как ограничение** —
  `prev_state` детачнут, ядро в `stable_physical_v2` без обучаемых параметров,
  последняя свёртка корректора zero-init, а Q_θ вообще не читает δ_phys. Отсюда же
  видно, что арм R0 несёт тот же корректор (обнулён только физический признак), то
  есть лестница контролирует ёмкость, и что контроль «Q_θ без уравнений» = конфиг
  R0 + `use_diabatic_term: true`.
  §7 переформулирует лестницу как **факторный план 2×2** (уравнения × Q_θ): три
  клетки посчитаны, четвёртая (X1 = R0 + Q_θ) нет, а без неё не отделить главный
  эффект Q_θ от **взаимодействия** с уравнениями. Разобрана гипотеза «Q_θ — это
  член замыкания, а не свободный источник»: конденсация в ядре строго отрицательна
  (δ ≥ 0, F > 0, ω < 0 там, где член включается), испарения и подсеточной конвекции
  нет вовсе ⇒ влажностный бюджет ядра **односторонний, ядро умеет только сушить** ⇒
  сухие уравнения дают признак с систематическим сухим смещением, что и объясняет
  измеренный вред A1 (+0.58 %) и то, почему внутри Q_θ основное несёт q-часть
  (−2.2 % против −0.74 % у t). Зафиксированы: пять механизмов с различающими
  диагностиками, неснятый конфаунд географии (Q_θ — единственная голова, читающая
  орографию и маску суши ⇒ нужен арм X2 с занулённым гео), бесплатная предпроверка
  на чекпоинте R3 (корреляция q-части Q_θ с конденсационным стоком) и следствия
  каждого исхода, включая новую мотивацию для эксперимента 19.

- **Модельная абляция «лестница уравнений» (эксперимент 16)** —
  `docs/experiments/16_model_ablation_ladder/`, конфиги `configs/ablation16/`.
  7 армов PI-IAM4VP на одинаковых данных/сиде: R0 (без физики) → R1 (легаси-
  hybrid) → R2a/R2 (A1/A2 до exp13) → R3 (+exp13) → R4 (+exp14) → R5 (+exp15).
  Порт принятых exp14/15-термов в `utils/physics_hybrid.PDE_kernel` как opt-in
  флаги (дефолты бит-в-бит, голден `tests/goldens/exp16_kernel_default_out.pt`,
  пины `tests/test_physics_hybrid_exp16_port.py`); проброс через `Models/IAM4VP`.
  Инструментарий `collect_metrics.py` (Comet→JSON, bulk) + `make_figures.py`
  (кривые + Δ-таблица снимком и по окну эпох, `tests/test_exp16_figures.py`).
  **Результат (сид 0, аудит устойчивости — README §8.5):** легаси-физика
  вредит структурно (потолок z ≈ 100 против 71 у baseline); главный полезный
  шаг — A-семейство (физкорректный passthrough + обучаемый диабатик Q_θ):
  устойчиво z −3…−4.4 %, t −2.5 %, u/v −3 %; exp13/14/15 — умеренные
  инкременты поверх (R5 лучший по окну: z −7.2 %); влажностный налог физики
  локализован в r 400–600 гПа и коррелирует с q-частью диабатика (приземная
  влажность улучшается); физика даёт sample efficiency, но при ~2.3–3.4×
  цене эпохи wall-clock выигрыша на этом масштабе нет.
  **Rollout-диагностика (README §8.6):** 12 шагов скользящим окном
  (free-running vs teacher-forced, чекпоинты общей эпохи 18, полная
  вал-2004) — `rollout_eval.py`/`rollout_figures.py`/`sh_files/
  abl16_rollout_eval.sh`, `tests/test_exp16_rollout.py`. Водораздел
  pre/post-exp13 — устойчивость rollout: во втором free-running окне
  pre-13 армы переворачиваются в плюс (R2a +3.5 %, R2 +1.5 %), post-13
  держат минус (R3 −1.6 %, R5 −2.2 %); короткие лиды выигрывают сильнее
  всего (z500@шаг 1 до −15 %). Сводка — §9, программа дальнейших
  экспериментов — §10.
  **exp16-long (t=12):** конфиги `configs/abl16_long/` — нативный горизонт
  12 кадров (`T_data=12`, вход 0–11 → прогноз 12–23), batch 64, lr 1e-3,
  2-суточная волна; армы `R3a` (без диабатика) и `R3q`
  (`diabatic_apply_to: t_only` — новое opt-in значение маски Q_θ,
  `tests/test_physics_hybrid_exp16_port.py::test_diabatic_mask_t_only...`).

- **Уравнения ядра: opt-in недостающая физика и структурные варианты
  (эксперимент 15)** в `utils/physics.py::PurePDEKernel` (дефолты бит-в-бит,
  запинены `Exp15EquationVariants`):
  - `latent_heating_coupling=True` — скрытое тепло конденсации в T:
    t_t += −(L/c_p)·(dq/dt|cond) (энергетическая связка Q₁/Q₂, Yanai 1973);
  - `omega_free=('t','q','u','v')` — исключение ω-зависимых членов из
    выходных тенденций: кинематическая ω на 1,4°/1 ч поточечно не коррелирует
    с требуемой (r < 0,13), её члены — шум амплитудой 1,4–2,7× тенденции;
    T-невязка 2,05→1,10 (глобус-2000), q 1,03→0,91 (впервые лучше нулевого
    прогноза); z всегда интегрирует полный t_t;
  - `newtonian_relaxation=True` (Held–Suarez 1994), `w_diagnostic='obrien'`
    (O'Brien 1970), `z_anchor='kinematic_ps'` (IFS ур. 2.8) — реализованы и
    ОТКЛОНЕНЫ измерением (документированные отрицательные результаты);
  - `rhs(sources=...)` — внешние климатологические источники Q₁/Q₂
    (строятся на 2000 годе, вне выборки 2001–2002 работают без потерь).
  Измерения и отчёт: `docs/experiments/15_deep_equation_improvement/`.

- **Уравнения движения: opt-in геометрия сферы и параметризации (эксперимент 14)**
  в `utils/physics.py` (дефолты бит-в-бит, запинены голденами):
  - `FiniteDifference`/`WENO5`/`PurePDEKernel(rows_south_to_north=True)` —
    ориентация оси широты под данные юг→север; легаси FD-4 `d_y` на данных
    WeatherBench возвращал −∂/∂y и переворачивал меридиональный барический
    градиент (невязка v 10–12 в эксп. 13 — следствие этого).
  - `GridConfig.latitudes_deg` и `GridConfig.lon_step_deg` — точная геометрия
    сетки: легаси `linspace`-широты не совпадают с широтами данных (глобус:
    шаг 1,3953° вместо 1,40625°; USA-кроп: сдвиг +1,5 ячейки), а легаси
    `pixel_x = 2πR·cosφ/W` на региональном кропе завышает шаг долготы в 4 раза
    (занижая ∂Φ/∂x и ω).
  - `PurePDEKernel(metric_terms=True)` — члены кривизны сферы
    +u·v·tanφ/a / −u²·tanφ/a (Holton & Hakim, ур. 2.19–2.20);
    `spherical_divergence=True` — метрический член −v·tanφ/a в континуити;
    `rayleigh_friction=True` — трение пограничного слоя Хелда–Суареса (1994);
    `vertical_scheme='lagrange3'` — точная вертикальная производная на
    нерегулярной сетке давления.
  Измерения на ERA5 2000–2002 (все часовые пары, USA + глобус):
  `docs/experiments/14_uv_residual_improvement/`.
- `tests/test_physics_sign_conventions.py::Exp14MomentumVariants` — пины
  знаков/уровней новых членов и голден дефолтного `PurePDEKernel.rhs`.
- `tests/test_physics_sign_conventions.py` — 15 регрессионных тестов знаковых
  конвенций (пины ζ=−p-конвенции + голдены нетронутых путей + фикс-тесты
  B1–B4).
- `docs/equations.md` — живая таблица уравнений ядра (эталон vs реализация,
  статус по членам); `docs/consistency.md` — таблица согласованности
  знаков/единиц по величинам и модулям. Обе обновляются в одном коммите с
  изменением уравнений.
- Пост-фикс статистика физики на ERA5-2000 (USA-кроп + глобус, 96 пар,
  cHARISMa jobs 4169155/4169160): скрипт
  `docs/experiments/13_sign_convention_fix/physics_stats_2000.py`, лончер
  `sh_files/physics_stats_2000_cpu.sh`, отчёт `PHYSICS_STATS_2000.md`.
  Ключевое: с mass-consistent ω исправленное ядро даёт T/z-невязку 2.4–7.3
  (до фикса 33–126), plain-ω вредна, «диабатика ×48» опровергнута
  (0.66–0.85×), CFL-нарушение — только полярные ряды глобуса.

### Changed

- **Политика чекпоинтов ([trainer.py](trainer.py)): на диск идут только `last.pt` и
  `best.pt`.** Пер-эпохные копии `epoch=NN-val_loss=X.pt` писались при каждом
  улучшении val и стоили полный payload (~470 МБ) каждая: за две волны abl16 плюс
  раны июля накопилось 1479 файлов на 275 ГБ, при том что кривые обучения живут в
  Comet, финальное состояние рана — `last.pt`, а отобранное по валидации —
  `best.pt`. Выбор путей вынесен в чистую `_checkpoint_paths()`; регрессия
  запинена `tests/test_trainer_checkpoints.py`. **Следствие:** сравнение армов на
  общей промежуточной эпохе больше не поднять из чекпоинтов — только на финальной
  (`last.pt`) или на отобранной (`best.pt`). После отзыва экспа 18 это скорее
  плюс: промежуточные эпохи вводили в заблуждение (см. экс. 16 §11.3).

### Fixed

- **Физика, знаковые конвенции вертикали (аудит 13, B1–B4)** в
  `utils/physics_hybrid.py` и `utils/physics.py`. `get_w` возвращает
  вверх-положительную w = −ω (интеграл `M_z` идёт от уровня к поверхности) —
  прежний код считал её равной ω:
  - B1: адиабатический член `get_t_t` брал ω с неверным знаком — подъём грел
    воздух вместо охлаждения; теперь `ω = −100·w`.
  - B2: гидростатический интеграл `get_z_t`/`get_z_zt` возвращал −Φ_t
    (прогрев столба опускал геопотенциал); знак подынтегрального исправлен.
  - B3: q-блок реконструировал давление как `p = ρRT` с ρ = −1/z_z
    (p = −14.8 Па вместо 50 000 Па → q_s срезалась в 1e-6, конденсация была
    мертва, триггер срабатывал на опускании). Теперь — реальные давления
    уровней, δ = (ω<0 ∧ q≥q_s), источник δ·F·ω/p (насыщенная псевдоадиабата);
    сигнатура `PurePDEKernel.get_q_dt` упрощена (z-производные не нужны).
  - B4: `PurePDEKernel._get_qs` использовал batch-global `_scale_tensor`
    (баг №4 exp 01, неисправленный в диагностической библиотеке) — заменён
    поэлементным `clamp(±20)`; `_scale_tensor` удалён.
  Ветки `legacy_paper`/`scale_diff`/`beta_plane` не тронуты (байт-в-байт).
  Детали и до/после: `docs/experiments/13_sign_convention_fix/`.
- `sh_files/remote_submit.sh`: раскрытие пустого `SBATCH_ARGS[@]` падало под
  macOS bash 3.2 c `set -u`.
