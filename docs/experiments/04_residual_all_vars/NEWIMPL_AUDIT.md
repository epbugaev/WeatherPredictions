# Аудит новой реализации (`utils/physics.py` `PurePDEKernel`) и план экспериментов по уменьшению невязки

Ветка `fix_inline_equations`. Все замеры — на реальном ERA5 (USA-кроп 32×64, снапшот 2003-07-15 12:00→13:00, dt=1ч) через sbatch на CPU-партиции `test`, env `weatherpred-refactor` (torch 2.3.1).

---

## 0. Резюме

Новая реализация `PurePDEKernel` — **численно грамотная и зрелая**: она чинит все 5 багов inline-ядра, добавляет region-aware геометрию, 4 интегратора (euler/rk4/ssp_rk3/semi_implicit с полноценным вертикально-модовым разложением), WENO-5, гипердиффузию и полярный фильтр, плюс диагностику (PDE-невязка, CFL, mass-divergence, геострофический баланс). Баг-фиксы задокументированы в docstring'ах (B1/B2/B4).

**Но главный вывод аудита численный и не в пользу «просто переключиться на новое ядро»:**

> **PDE-невязка одного шага ВЕЗДЕ ≫ 1** (rel_L1: t = 22–60, z = 60–167, u/v = 3–11). То есть упрощённая PDE-форма — при любой геометрии, границе и интеграторе — объясняет **меньше, чем сам инкремент** реального ERA5. Ограничение — **в форме уравнений**, а не в численной схеме и не в геометрии.

Это переворачивает приоритеты: чинить интегратор/границу бессмысленно, пока невязка формы на порядок больше сигнала. Ниже — что именно ограничивает и какие эксперименты дадут результат.

---

## 1. Что нового и корректно в `PurePDEKernel`

| Компонент | Что делает | Статус |
|---|---|---|
| **Region-aware `Grid`** | `lat_range_deg` → `pixel_x=2πR·cosφ/W`, `pixel_y`, `f=2Ω·sinφ` на реальных широтах | ✅ Корректно. **Но по умолчанию `(-90,90)`** и почти нигде не передаётся (только `sanity_lat_range.py`) |
| **Кориолис** | 3 режима: `constant`(2Ω·sin45°), `beta_plane`, `spherical`(2Ω·sinφ) | ✅ Исправлен (inline был `f=Ω`) |
| **Температурная тенденция** | `adiabatic_omega`: `R_d·T·ω/(c_p·p)`, ω=100·w | ✅ Исправлена (inline: −2488·z_z·w) |
| **Гидростатика** | `R_eff=R_d` по умолчанию (`use_universal_R` восстанавливает баг) | ✅ Исправлена (inline: R=8.314) |
| **Границы** | `boundary_x/y/z` раздельно; periodic/replicate/reflect; правильное замыкание | ✅ Гибко (inline: везде periodic) |
| **Интеграторы** | euler, rk4, ssp_rk3, semi_implicit (верт.-модовый Гельмгольц, масс-взвеш. симметризация) | ✅ Реализованы корректно |
| **WENO-5** | Jiang–Shu, d-веса [0.1,0.6,0.3] | ✅ |
| **Стабилизаторы** | implicit-spectral гипердиффузия (B1-фикс), полярный фильтр | ✅ |
| **Диагностика** | `pde_residual`, `cfl_number`, `mass_divergence`, `geostrophic_residual`, `potential_vorticity_proxy` | ✅ Позволяет честно мерить невязку |

**Вывод по разделу:** как физический движок-референс — это правильная замена inline. Проблема не в реализации, а в том, какую физику она (как и inline) реализует.

---

## 2. Численные результаты аудита

### 2.1 PDE-невязка одного шага (панель a) — ГЛАВНОЕ

r = (X_{t+1}^ERA5 − X_t^ERA5)/dt − RHS(X_t^ERA5), rel_L1 к |Δданные|/dt:

| Геометрия / граница | t | z | u | v |
|---|---:|---:|---:|---:|
| global / periodic | 23.8 | 67.0 | 3.3 | 5.2 |
| global / replicate | 21.5 | 60.4 | 3.1 | 5.2 |
| **usa / periodic** | **59.9** | **167.5** | 5.3 | 11.1 |
| **usa / replicate** | **59.6** | **166.8** | 5.2 | 11.1 |

**Все значения ≫ 1** — невязка больше самого шага. Форма RHS не воспроизводит эволюцию ERA5.

### 2.2 Region-aware геометрия НЕ уменьшает невязку (контринтуитивно)

USA-геометрия (25–55N, физически корректный `pixel_y`) даёт **бОльшую** невязку, чем глобальный дефолт (t: 60 vs 22). Причина: с корректным (меньшим) `pixel_y` пространственные градиенты и, значит, адвективные тенденции RHS **больше**, а раз форма неверна — расхождение с реальным Δ тоже больше. То есть **исправление геометрии само по себе не улучшает предсказание** — оно лишь делает уравнения «честнее неправильными». Это отдельный аргумент, что узкое место — форма.

*(Замечание аудитора учтено: fixed-global-grid inline-ядра — реальный физический mismatch с региональными данными; но, как показывает этот тест, его исправление невязку не снижает — эффект есть только в паре с исправлением формы уравнений.)*

### 2.3 Граница periodic vs replicate — почти без эффекта

Δневязки < 1% (t: 59.9 vs 59.6). Хотя periodic по долготе для USA-окна физически неверен, вклад в невязку одного шага пренебрежимо мал по сравнению с ошибкой формы.

### 2.4 Интеграторы: высокий порядок НЕ помогает (панель b)

Шаг взрыва pure-physics rollout (dt=300с, из 48):

| Интегратор | global/per | usa/rep |
|---|---:|---:|
| euler | 7 | 8 |
| **rk4** | **4** | **4** |
| **ssp_rk3 (weno5)** | **4** | **5** |
| semi_implicit | 7 | 8 |

Схемы высокого порядка (rk4, ssp_rk3) взрываются **раньше** euler. Это классический признак, что неустойчивость — **нелинейно-CFL-типа** (grid-scale энергия на реальном ERA5-спектре), а не ошибка усечения по времени. semi_implicit и euler держатся чуть дольше, но принципиально проблему не решают. Согласуется с dt-sweep (12→17→25→42 при dt 300→150→75→37.5) — сублинейно.

### 2.5 Состояние не геострофично в упрощённой проекции (панель c)

Геострофическая невязка |f·v − ∂z/∂x| / |f·v| ≈ 0.84–0.97, |f·u + ∂z/∂y| / |f·u| ≈ 1.1–2.4. На средних широтах атмосфера должна быть ~геострофична (невязка ≪ 1). Здесь она ~1 — то есть даже базовый баланс, зашитый в RHS, не выполняется на реальных полях. Это прямой источник большой невязки уравнений импульса.

---

## 3. Почему невязка большая — диагноз

Упрощённая «сухая» PDE-форма (общая для inline и `PurePDEKernel`) физически неполна:

1. **Нет диабатических источников.** Радиация, конвекция, скрытое тепло осадков, поверхностные потоки — всё это крупные члены в реальном dT/dt и dq/dt. RHS их не содержит → невязка t/q обязана быть большой.
2. **Диагностическое w из 2D-неразрывности** (`w_z=−u_x−v_y`, интегрируется по вертикали) — грубое приближение; реальное ω в ERA5 включает орографию и диабатику. Ошибка w напрямую бьёт по t_t (через ω) и z_t (через гидростатику) → отсюда z-невязка 60–167, худшая из всех.
3. **Геострофический дисбаланс** (§2.5): подсеточные и агеострофические члены не разрешены на 32×64.
4. **Нет диффузии/трения** в базовом RHS (гипердиффузия — опционный стабилизатор, не физический член).
5. **Разрешение 32×64** обрезает спектр; grid-scale накопление → нелинейная неустойчивость (§2.4).

Пункты 1–2 доминируют для t/z/q; 3–5 — для устойчивости rollout.

---

## 4. План экспериментов по уменьшению невязки (по приоритету)

**P1. Разложить невязку по членам и по регионам (ocean vs mountains).**
Замерить вклад каждого члена RHS (adv, Coriolis, hydrostatic, adiabatic-ω) в полную невязку по маскам: океан, суша, горы (орография > порога), тропики/средние широты. Ожидание из §2.5 и §3.2: невязка z/w максимальна над орографией, невязка t — в тропиках (конвекция). **Это ваш запрошенный региональный аудит невязки — следующий коммит.**

**P2. Добавить недостающие физические члены как обучаемые источники.**
Раз сухая форма даёт невязку ≫1, самое эффективное — не «лучше интегрировать», а **дополнить RHS обучаемым диабатическим членом** S_θ(X): dX/dt = RHS_phys(X) + S_θ(X). Это ровно ваша residual-corrector идея, но в пространстве **тенденций**, а не выхода. Проверить, падает ли невязка после обучения S_θ на паре ERA5(t)→ERA5(t+1).

**P3. Заменить диагностическое w на mass-consistent / данные ω из ERA5.**
`w_diagnostic="mass_consistent"` уже есть в ядре. Сравнить невязку z/t с plain-w. Если ERA5 содержит вертикальную скорость — подать её напрямую и измерить, сколько z-невязки снимается.

**P4. Балансировка начального состояния перед rollout** (не для невязки, для устойчивости).
`balance_initial_state(mode=geostrophic/dfi)` — но прошлый тест показал, что она **не** сдвигает шаг взрыва (geostrophic даже хуже). Оставить как контрольный, приоритет низкий.

**P5. Region-aware геометрия — включить, но не ждать снижения невязки.**
Передавать `lat_range_deg=(24,56)` для USA в продакшн-путь (сейчас дефолт global). §2.2 показал: сам по себе эффекта на невязку нет, но для **физической корректности** обучения (правильный Кориолис/шаги на широтах данных) это нужно сделать — эффект проявится только в паре с P2.

---

## 5. Что это значит для стратегии «фикс → PurePDEKernel»

- Фикс 5 багов (сделан) убирает катастрофические 10⁶× ошибки — это необходимое условие, чтобы «чистая физика» вообще была конечной. **Но невязка ≫1 остаётся** — фикс не делает физику точной, он делает её не-взрывной и физически осмысленной по единицам.
- Переключение продакшн-моделей на `PurePDEKernel` даёт: правильные уравнения + region-aware геометрию + честную диагностику невязки. Это **инфраструктура** для P1–P3, но **само по себе качество предсказания не улучшит** — упрощённая форма остаётся упрощённой.
- Реальный выигрыш — в P2 (обучаемый диабатический член) поверх исправленного физического RHS. Это и есть научная новизна: не «физика как фиксированный prior», а «физика + обучаемая невязка», где физика уже без багов.

---

## Приложение: воспроизводимость

- Скрипт: `tools/newimpl_audit.py`. Env: `/home/ebugaev/.conda/envs/weatherpred-refactor/bin/python` (torch 2.3.1), `PYTHONNOUSERSITE=1`.
- Запуск: sbatch, партиция `test`, аккаунт `proj_1715`, 8 CPU, job 4158693, узел cn-044, COMPLETED.
- Данные: `/home/ebugaev/era5_memmap/predformer_usa_2000_2004.dat` (почасовой, shape [43848,69,32,64]), снапшот h=4692 (2003-07-15 12:00) и h+1.
- Результаты: `newimpl_audit.json`. Фигура: `newimpl_audit.png`.
