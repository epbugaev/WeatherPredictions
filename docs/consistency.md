# Таблица согласованности: знаковые конвенции и единицы

**Назначение.** Сводка конвенций (знак, единицы, ориентация осей) каждой
физической величины по всем местам употребления. Цель — чтобы ошибка класса
«прошлый аудит прочитал интеграл в другую сторону» (аудит 13) ловилась чтением
одной таблицы. **Правило ведения:** новая величина или новый потребитель
существующей — новая строка/пометка здесь в том же коммите; конвенции
запинены тестами `tests/test_physics_sign_conventions.py` (класс
`HybridConventionPins`).

## 1. Базовые конвенции осей и операторов

| Объект | Конвенция | Проверка (тест/проба) |
|---|---|---|
| Ось уровней (индекс 0→12) | 50 → 1000 гПа: индекс растёт **с давлением** (вниз по высоте) | `PRESSURE_LEVELS_HPA`, буфер `pressure` |
| Ось H (широта) в данных | **верифицировано по nc (эксп. 14):** lat WeatherBench 1.40625° = −89,297…+89,297 по возрастанию — строка 0 юг, индекс растёт на север; USA-кроп rows 75:107 = 16,17…59,77° с.ш. | чтение координат nc (журнал эксп. 14); T500 на краях ≈ 234 K, в середине ≈ 268 K |
| `d_y`: WENO-пути (оба модуля) | +∂/∂j = **+∂/∂y** на данных юг→север — корректно | пин `Exp14MomentumVariants.test_d_y_orientation...` (FD-4-ветка) |
| `d_y`: легаси FD-4 (`physics.FiniteDifference`) | **−∂/∂j = −∂/∂y на этих данных** — перевёрнут (исторический рассинхрон с WENO); ломал v-уравнение диагностики (невязка v 10–12). Для сырых данных обязательно `rows_south_to_north=True` (эксп. 14) | тот же пин; невязка v: 12,0 → 1,4 на глобусе-2000 |
| Ось W (долгота) | индекс растёт на восток; d_x = +∂/∂x | WENO-пин на sin |
| Широты/шаг `Grid` (легаси) | ⚠️ `linspace(lat_range, H+2)[1:-1]` ≠ широты данных (глобус: шаг 1,3953° вместо 1,40625°; USA: сдвиг +1,5 ячейки — «латентно-центровая» широта 18,28° применена к полноразрешённому кропу); `pixel_x = 2πR·cosφ/W` верен только для глобального W (на USA-кропе dx ×4) | точная геометрия: `latitudes_deg`, `lon_step_deg` (эксп. 14); пин `test_grid_explicit_latitudes` |
| `d_z` (оба модуля) | **−∂/∂p** (= ∂/∂ζ, ζ=−p): стенсиль `[-1,8,0,-8,1]` — минус FD-4 | `d_z(p) ≈ −1` (verify-скрипт, проба 1) |
| `integral_z` / `M_z` | Σ_{j≥i} = **∫ от уровня к поверхности** (∫_p^{p_s}) | `integral_z(1)`: 1012 гПа наверху, 75 внизу (проба 2) |
| `get_w` | w = −∫_p^{p_s} div dp′ = **−ω**, вверх-положительная, **гПа/с** | дивергенция ⇒ w<0 (пин-тест) |
| WENO reflect-граница | производная в узле 0 оси ≡ 0; паддинг влияет на 2 крайние ячейки | аудит 13 §4; метрика `physics_residual_delta_edge_interior_ratio` |

## 2. Величины: единицы и знаки по потребителям

| Величина | Единицы | Производитель | Потребители и требуемая конвенция | Согласовано |
|---|---|---|---|---|
| z (канал данных 4:17) | геопотенциал Φ, **м²/с²** (не гпм!) | ERA5/WeatherBench | PGF `−z_x` (м/с²) ✅; гидростатика ✅; капы z в м²/с² ✅ | ✅ |
| w из `get_w` | −ω, гПа/с | `get_w` | верт. адвекция `d_z(X·w)`/`−w·X_z`: двойная ζ-инверсия гасится ✅; адиабата и конденсация обязаны брать **ω = −100·w [Па/с]** ✅ (B1/B3) | ✅ после аудита 13 |
| ω | Па/с, >0 при опускании | `−100·w` | `t_t_adia = +R_d·T·ω/(c_p·p_Па)` ✅; триггер конденсации δ: **ω<0** ✅; источник `δ·F·ω/p_Па` ✅ | ✅ |
| t_t | K/с | `get_t_t` | `get_z_zt`: подынтегральное **+R_eff/p_гПа·t_t** (интеграл вниз, анкер Φ_t(p_s)=0) ✅ (B2) | ✅ |
| p (давление уровней) | буфер `pressure`: **гПа** (physics_hybrid, module-level) / **Па** (`Grid.pressure`) | константы | адиабата: ×100 → Па ✅; гидростатика physics_hybrid: гПа согласованно с M_z(гПа) ✅; physics.py: /100 → гПа ✅; Магнус: **Па** ✅ | ✅, но ⚠️ разные единицы буферов между модулями — при переносе кода конвертировать явно |
| `pixel_z` | Δp между уровнями, **гПа**, центрированные | константы | `d_z` (…/гПа), `integral_z` (×гПа), mass_consistent-веса | ✅ |
| `pixel_x`, `pixel_y` | метры; pixel_x ∝ cosφ реального кропа | `__init__`/`Grid` | все горизонтальные производные | ✅ physics_hybrid (dx = R·cosφ·Δλ явно); ⚠️ `Grid`-легаси: dx = окружность/W — на кропе ×360/(W·Δλ); передавайте `lon_step_deg` (эксп. 14) |
| tanφ/a (эксп. 14) | 1/м, буфер `tan_phi_over_a` (1,1,H,1) | `PurePDEKernel.__init__` при `metric_terms`/`spherical_divergence` | кривизна: `+tanφ/a·u·v` в u_t, `−tanφ/a·u²` в v_t ✅; континуити: div −= v·tanφ/a ✅ | ✅ (пины `Exp14MomentumVariants`) |
| k_v Рэлея (эксп. 14) | 1/с, буфер `rayleigh_k` (1,P,1,1); k_f = 1/сут, σ_b = 0,7 | `PurePDEKernel.__init__` при `rayleigh_friction` | `−k_v·u`, `−k_v·v` в get_uv_dt; ноль при p ≤ 700 гПа | ✅ (пин: ноль выше σ_b, −k_f·u на 1000 гПа) |
| Матрица `dz_lagrange` (эксп. 14) | 1/гПа, (P,P); знак — конвенция d_z = −∂/∂p | `_lagrange3_dz_matrix` при `vertical_scheme='lagrange3'` | `_d_z` вместо стенсиля: точна на нерегулярной p-сетке (пин: d_z(2p) = −2) | ✅ |
| `block_dt` | секунды; 3600/(depth·steps)=400 c (v4) | конфиг → `PDE_kernel` | приращения `tendency·dt`; капы — на один подшаг | ✅ |
| q (канал 30:43) | в данных — **r, %**; в ядре — **q, кг/кг** | конверсия r↔q (Магнус, Па) в `_physics_prior_from_state` | ядро всегда видит кг/кг при `relative_to_specific` (v4-армы) ✅; кап q=2e-3 кг/кг ✅ | ✅; ⚠️ режим `as_is` подаёт r(%) в уравнения для q — только для legacy-регрессий |
| q_s | кг/кг | `get_qs`/`_get_qs`/`saturation_specific_humidity` | все три реализации: e_s Па, p Па, поэлементный clamp (±20) | ✅ после B4; ⚠️ clamp занижает крутизну e_s при T<−35 °C |
| f | с⁻¹, 2Ω·sinφ | буфер `f_field` | `+f·v` в u_t, `−f·u` в v_t | ✅ |
| Приращения `_limit_increment` | физ. единицы за подшаг; **`.detach()`** | ядро | эволюция полей: физика — frozen-фича, градиент через состояние = identity | ✅ (осознанный дизайн) |
| Нормировка NN↔физика | z-score по каналам (`physics_data_mean/std`) | `set_physics_normalization` | денорм → физшаг → ренорм; клип тенденции 8σ/ч | ✅ (stable_physical_v2; дефект B закрыт) |
| T_eq, k_T (Held–Suarez, эксп. 15) | K; 1/с | буферы `hs_t_eq`, `hs_k_t` (`newtonian_relaxation=True`) | `get_t_t`: −k_T·(T−T_eq); T_eq ∈ [200, 315] K | ✅ (пин `Exp15EquationVariants`) |
| Конденсационный сток `_condensation_source` | (кг/кг)/с, ≤ 0 при конденсации | `physics._condensation_source` | `get_q_dt`: слагаемое q_t ✅; `latent_heating_coupling`: t_t += **−(L/c_p)·cond** (сушит → греет) ✅ | ✅ |
| Баротропный якорь z (эксп. 15) | м²/с³; ∂p_s/∂t [Па/с] = 100·w_top | `_raw_column_w_top` (сырая кинематика, БЕЗ mc/obrien) | `get_z_t(baro=...)`: добавка ко всем уровням; конвергенция → z_t выше | ✅ (пин-тест); ⚠️ не использовать w_top из mc/obrien-ω — они зануляют его по построению |
| ω О'Брайена (эксп. 15) | гПа/с | `get_w` при `w_diagnostic='obrien'` | w(p_top) = 0 точно, w(p_s) не тронута; вес ((p_s−p)/(p_s−p_top))² | ✅ (пин-тест) |
| Внешние источники `rhs(sources=...)` | единицы тенденции соответствующей переменной | климатологии эксп. 15 (строятся на 2000) | добавка к `{u,v,t,q,z}_t` после всех членов | ✅ (пин-тест точного сложения) |

## 3. Межмодульная согласованность реализаций

| Соотношение | physics_hybrid (прайор) | physics.py (диагностика) | Совпадение |
|---|---|---|---|
| адиабата ω-конвенция | `−100·w` (`get_t_t:497`) | `−100·w` (`get_t_t:818`) | ✅ |
| гидростатика знак | `+R_eff/p_гПа` (`get_z_zt:513`) | `+R_eff/p_гПа` (`get_z_t:825`) | ✅ |
| конденсация | реальные p, δ(ω<0∧q≥q_s), `δ·F·ω/p` (`get_q_dt:554`) | то же (`get_q_dt:881`) | ✅ |
| Магнус | clamp ±20 поэлементно | clamp ±20 поэлементно (`_get_qs:857`) | ✅ (B4 закрыл расхождение) |
| semi-implicit reference | — | `_si_setup` зеркалит цепочку с фиксами (матрица A численно прежняя — два флипа гасятся) | ✅ |
| legacy-ветки | `legacy_paper`, `scale_diff`, `beta_plane` | те же флаги | 🔒 байт-в-байт, заведомо сломаны, только для регрессий |

**История:** до 2026-07-08 строки «адиабата», «гидростатика», «конденсация»,
«Магнус (physics.py)» были рассинхронизированы с физикой (баги B1–B4,
[эксперимент 13](experiments/13_sign_convention_fix/README.md)).
